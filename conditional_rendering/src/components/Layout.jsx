//in this component we will use our props to render a component via an If Else Statement
//first, lets make sure we import our 2 components in here!
//remember, Return Statements go inside of the If Else!

const Layout = () => {
    //code goes here!
    return {}
}