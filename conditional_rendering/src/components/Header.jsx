//for our Header, we are going to return our data based on a Ternary!
//lets make sure to add our Props here!

//pseudocode :
// if we are Logged In (isLoggedIn is true), we will welcome our user with a greeting
//if we are not logged in, we will have "Create Account" rendered in the header

const Header = () => {
    return (
        //code goes here!
    )
}

export default Header
